<div class="container">
  <div class="row">
    <div class="col-lg-8 left-side">
      {{#each model as |arrow|}}
        {{comment-box arrow=arrow}}
      {{/each}}
    </div>
    <div class="col-lg-4 fixed-input">
      {{outlet}}
    </div>
  </div>
</div>

{{#link-to "arrows.new" class="btn btn-lg arrow-btn"}}Create arrow{{/link-to}}

<style media="screen">
  .arrow-btn{
    bottom: 0;
    right: 0;
    position: fixed;
    border: 1px solid black;
  }
</style>
