<form {{action 'createArrow' on="submit"}}>
  {{input type="text" value=arrowContent placeholder="Email"}}
  {{input type="bool" value=arrowRead placeholder="Password"}}
  <button type="submit">Create a new arrow</button>
</form>

{{outlet}}
